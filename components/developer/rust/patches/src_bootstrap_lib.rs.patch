$NetBSD: patch-src_bootstrap_lib.rs,v 1.2 2018/01/08 09:33:58 ryoon Exp $

Don't filter out optimization flags.

Updated to rust 1.41

--- rustc-1.41.0-src/src/bootstrap/lib.rs.orig  2020-06-11 15:06:39.368707480 +0000
+++ rustc-1.41.0-src/src/bootstrap/lib.rs       2020-06-11 15:06:57.213233987 +0000
@@ -752,7 +752,6 @@
         // cc-rs because the build scripts will determine that for themselves.
         let mut base = self.cc[&target].args().iter()
                            .map(|s| s.to_string_lossy().into_owned())
-                           .filter(|s| !s.starts_with("-O") && !s.starts_with("/O"))
                            .collect::<Vec<String>>();
 
         // If we're compiling on macOS then we add a few unconditional flags
